# Changelog

## [0.10.0] - 2026-09-02

### 추가
- **사이드바 폴더 트리** — 작업공간·라이브러리를 한 트리에 2섹션으로 표시. 점선 버튼 + 팝업 피커 방식을 대체
  - 폴더 아이콘 색상으로 영역 구분 (작업공간 주황 / 라이브러리 파랑)
  - 지연 로딩 + 캐시, 영역별 새로고침
- **폴더 인스펙터 패널** — 폴더 정보·이름변경·오디오 파일명·커버 이미지·앨범카드·LRC를 우측 패널로 통합
  (본문 파일 테이블에는 파일명 컬럼이 없어 파일명 확인은 여기서)
- **모바일 폴더 바텀시트** — 홈의 `폴더 열기` 버튼 하나로 70vh 시트에 같은 트리를 표시
- 자동태깅 `covers` 옵션 — `embed`(기본) / `embed+folder` / `none`
- 자동태깅 `search_source` 옵션 — `tags_fallback`(기본) / `filename` / `tags`
- 자동태깅 `request_delay_ms` — provider 요청 간격 (기본 400ms)
- 캐시 예산 환경변수 `CACHE_MEMORY_MB` (기본 128MB)

### 수정
- **심볼릭 링크로 연결된 폴더가 403이던 문제** — README가 권장하는 NAS 설치 방식
  (`ln -s /volume1/music ./data/library/...`)에서 폴더가 목록에 안 나오거나 클릭 시 403이 나던 문제.
  경로 검증을 `app/core/path_guard.py`로 통일하고, `..` 이탈은 계속 차단
- **파일 단위 자동태깅이 컴필레이션 폴더에서 폴더 커버를 덮어쓰던 문제** — 트랙마다 폴더 커버를
  지우고 다시 써 마지막 트랙 커버만 남았다. 이제 기본값은 파일 임베드만
- 파일명 파싱 실패 시 검색조차 하지 않고 건너뛰던 문제 — 기존 태그로 폴백
- 자동태그 모드가 열 때마다 `앨범`으로 초기화되어 파일 단위 모드를 찾기 어렵던 문제 — 마지막 모드 기억

### 성능
- 백엔드 캐시 상한을 **항목 수 → 바이트**로 전환. 기존에는 파일 1,000개 폴더(2.6MB) 500개로
  최악 1.3GB까지 커질 수 있었다 (Synology DS220j RAM 512MB)
- 프론트 sessionStorage 캐시 축출 정책 추가. 기존에는 용량(약 5MB)이 차면 그 세션 내내
  캐시가 조용히 전혀 동작하지 않았다

### 제거
- `LibraryPickerModal` — 트리·바텀시트 도입으로 참조가 사라짐

### 알려진 제한
- 재귀 보기(하위 폴더 전체)는 심볼릭 링크를 통과하지 못한다 (`os.walk` 기본값)
- 하위 폴더가 매우 많은 폴더는 NAS에서 로딩이 느릴 수 있다 (`_quick_scan` N+1 — 후속 과제)

## [0.9.8] - 2026-09-01

### 수정
- 모바일 하단 탭의 **태깅** 버튼으로 진입할 때 마지막 작업 폴더를 복원하도록 수정
  - 저장된 폴더가 없으면 `browser.resetFolder()`로 초기화해, 시작 시 자동선택된 workspace 루트 목록이 남아 보이던 문제 해결
- 앱 시작 시 `startup_folder` 자동선택이 마지막 열기 폴더(`LAST_FOLDER_KEY`)를 덮어쓰던 문제 수정 (`_skipLastFolder` 가드 추가)

### 추가
- `stores/browser.js`에 `resetFolder()` 추가 — 선택 폴더·파일 목록·체크 상태·재귀 모드 등 브라우저 상태 일괄 초기화

## [0.8.25] - 2026-04-15

### 수정
- 모바일에서 폴더 선택 시 직접 오디오 파일이 없고 오디오가 있는 하위 폴더만 있는 경우, 파일 목록 대신 폴더 그리드만 표시되던 버그 수정
  - `loadFiles()` 완료 후 직접 파일이 0개이고 `has_audio` 하위 폴더가 있으면 자동으로 재귀 로드로 전환
- `loadRecursiveFiles()` 내 `isRecursiveMode` 미완료 스캔 재시도 폴링이 동작하지 않던 버그 수정 (`false` → `true`)

## [0.4.0] - 2026-03-19

### 추가
- 모바일 최적화: Browser, Settings, MiniPlayer, RenameByTagsModal 반응형 레이아웃
- 파일명 변경 패턴 히스토리: 최근 사용 패턴 콤보박스로 선택 (localStorage, 최대 15개)
- HTML 내보내기: 앨범/폴더 단위 독립형 HTML 앨범 쇼케이스 페이지 생성
- 활동 로그 페이지: 태그 저장·파일명 변경·LRC 검색·로그인 등 시스템 전반 로그 확인 (Settings > 활동 로그)
- 라이브러리 LRC 검색: 이동할 폴더의 정리된 파일에 LRC 일괄 검색 (Settings > 라이브러리 LRC)

### 변경
- Settings 탭 네비게이션: 모바일 상단 가로 스크롤, 데스크톱 좌측 세로 탭
- Browser 파일 목록: 모바일 카드뷰 / 데스크톱 테이블 이중 레이아웃

## [0.3.0] - 2026-03-08

### 추가
- 자동 스캔 스케줄러 (APScheduler): `scan_interval_minutes` 설정 연동, 설정 변경 시 즉시 반영
- Spotify 메타데이터 검색: Client Credentials 인증, 트랙/앨범 검색, 커버 URL 다운로드 후 자동 저장
- 메타데이터 일괄 적용: 앨범 단위 Spotify 트랙 목록 조회 → track_no 매칭으로 로컬 트랙에 적용
- 메타데이터 소스 스텁: Apple Music, Melon, Bugs (추후 스크립트 연동 예정)
- 설정 페이지: Spotify API 키 입력, 스케줄러 상태 표시, 즉시 스캔 버튼
- 앨범 상세 페이지: 🔍 메타데이터 검색 버튼 → MetadataSearchModal
- Alembic 마이그레이션 0004 (metadata provider config keys)

## [0.2.0] - 2026-03-08

### 추가
- 설정 페이지 (Settings): 스캔 주기, 커버아트 추출, 지원 포맷, 언어 설정
- DB 기반 설정 저장 (`app_config` 테이블)
- 스캔 이력 로그 DB 저장 (`scan_log` 테이블)
- 파일 로그: `app.log`, `error.log` (RotatingFileHandler, 10MB × 5)
- 백업/복원: `pg_dump` 기반 tar.gz 아카이브 (DB + 커버아트)
- 버전 관리: `version.py` 단일 파일, `/api/health` 버전 응답
- Alembic 마이그레이션 0002 (app_config), 0003 (scan_log)

## [0.1.0] - 2026-03-08

### 추가
- 초기 릴리즈
- 앨범 / 아티스트 / 트랙 라이브러리 관리
- 음악 폴더 스캔 (MP3, FLAC, M4A, OGG)
- 커버아트 자동 추출 및 업로드
- 트랙 태그 편집 (개별 / 일괄)
